<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:orientation="vertical" android:background="@miui:drawable/settings_background" android:layout_width="fill_parent" android:layout_height="fill_parent"
  xmlns:android="http://schemas.android.com/apk/res/android">
    <ListView android:id="@android:id/list" android:layout_width="fill_parent" android:layout_height="0.0dip" android:layout_weight="1.0" style="@miui:style/Widget.ListView.NoDivider" />
    <LinearLayout android:id="@id/list_btn_bar" style="@miui:style/BottomButtonBar">
        <Button android:id="@id/btn_complete" android:text="@android:string/ok" style="@miui:style/BottomButton.Left" />
        <Button android:id="@id/btn_cancel" android:text="@android:string/cancel" style="@miui:style/BottomButton.Right" />
    </LinearLayout>
</LinearLayout>
